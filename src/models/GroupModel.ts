// GroupModel.ts
// Định nghĩa kiểu dữ liệu nhóm giao dịch

export interface GroupModel {
  id: string;
  name: string;
  color: string;
  createdAt: string; // ISO string
  // Thêm các trường khác nếu cần
} 